# T-20260211-100 审查证据包（整改版）

> 证据对应版本：`5300f5f7722192b3f6ae51786293ae1723357002`（若后续有新提交，以最新 hash 为准）

## 1) 仓库与提交信息
- 仓库 URL: https://github.com/ldj518/lzh20260211
- 默认分支: `main`
- 最新提交（当前）：`5300f5f7722192b3f6ae51786293ae1723357002`
- 审查可访问性: 仓库当前为 Public，可直接访问。

## 2) 部署与可用性
- Pages URL: https://lzh20260211.pages.dev
- 在线探测结果：HTTP 200（`web_fetch` 检查通过）
- 页面关键文案可读：学科/难度/练习时长/家长端统计（与当前版本一致）

### 自定义域（`*.5208090.xyz`）整改状态
- 已发起绑定：`hw.5208090.xyz`（Cloudflare Pages API 返回 success）
- 当前状态：`pending`
- 阻塞原因：`CNAME record not set`
- 说明：当前 Cloudflare Token 无 Zone DNS 写权限（尝试写入 CNAME 返回 Authentication error）。
- 待执行动作（陛下/有DNS权限账号）：
  1. 在 `5208090.xyz` 增加 CNAME：`hw` → `lzh20260211.pages.dev`（建议 Proxied）
  2. 等待证书签发后再次验证 `https://hw.5208090.xyz`

## 3) 功能对照（入口-操作-结果）
| 功能 | 入口 | 操作 | 结果 |
|---|---|---|---|
| 题目录入 | `data/homework.json` | 增/改题目 JSON | 页面自动读取并出题 |
| 学生作答 | 首页 `开始练习` | 选择学科/难度/时长后答题 | 进入题目页可逐题作答 |
| 提交判分 | 题目页 `提交答案` | 提交选择/填空/简答 | 自动给出对错与解析 |
| 结果页 | 完成题目或计时结束 | 自动跳转 | 显示得分、正确率、错题 |
| 计时 | 首页 `练习时长` | 选10/20/30分钟 | 顶部显示倒计时，到时自动交卷 |
| 错题本 | 结果后 `仅看错题` | 启动错题复习 | 仅载入错题集合 |
| 主观题建议评分 | 简答题提交 | 输入答案后提交 | 显示建议评分（家长参考） |
| 家长端统计 | 首页 `家长端统计（本机）` | 完成多轮练习 | 显示累计次数/平均正确率/最近成绩 |

> 运行截图说明：当前运行环境缺少浏览器动态库，无法在 VPS 内自动截图；如需审查截图，请由陛下在移动端按同一版本补充“首页/答题页/结果页”截图。

## 4) 隐私与合规（全链路扫描）
- 姓名策略：已脱敏为“路*昊同学”（`index.html`、`README.md`）。
- 原题图片策略：仓库仅保存结构化题库 `data/homework.json`，不提交原始题图。
- 仓库可见性策略：建议私有；公开时必须持续脱敏。

### 仓库检索命令（可复核）
```bash
# 检查是否出现未脱敏姓名（应无输出）
grep -R --line-number --exclude-dir=.git '路则昊' .

# 检查脱敏形式（应命中 index.html/README.md）
grep -R --line-number --exclude-dir=.git '路\*昊' .
```
